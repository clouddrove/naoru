{
  "name": "naoru",
  "version": "0.2.2",
  "description": "Heal your broken CI with AI — diagnoses why your pipeline failed and comments the fix on your PR.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "test": "vitest run",
    "build": "ncc build src/index.js -o dist --minify",
    "build:cli": "ncc build src/cli.js -o dist-cli --minify",
    "lint": "eslint src test"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "@anthropic-ai/sdk": "^0.110.0",
    "openai": "^6.45.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "vitest": "^4.1.9"
  }
}
